import { Box } from "@mui/material";
import React from "react";

type Props = {};

const LoginComp = (props: Props) => {
  return (
    <Box className="w-screen border h-screen flex items-center justify-center">
      <Box></Box>
    </Box>
  );
};

export default LoginComp;
